# Monorepo 架构学习路线：以 Vben Admin Pro 为参考

这份文档面向“从 0 开始理解 monorepo 架构”的学习路径。目标不是一口气抄出 Vben Admin Pro，而是逐层理解它为什么这样设计：`package.json` 负责什么，`pnpm-workspace.yaml` 解决什么，`tsconfig` 如何分层，ESLint 配置包为什么要构建成 `dist`，以及 `internal/*` 这类内部包如何支撑整个仓库。

你可以把这份文档当成路线图。学习顺序很重要，前面的基础不清楚，后面的配置会像“魔法字符串”；按顺序学，配置会逐渐变成可推导的工程设计。

## 1. 总体目标

学完后你应该能做到：

- 看懂一个现代 monorepo 的目录分层。
- 理解根 `package.json`、子包 `package.json`、`pnpm-workspace.yaml` 的职责边界。
- 知道 `dependencies`、`devDependencies`、`peerDependencies`、`workspace:*`、`catalog:` 分别用于什么场景。
- 理解 ESM、CJS、`type: "module"`、`exports`、`main`、`types`、`dist` 的关系。
- 能设计共享 `tsconfig` 包，例如 `@xxx/tsconfig`。
- 能设计共享 ESLint 配置包，例如 `@xxx/eslint-config`。
- 能理解为什么内部工具包需要 `stub`、`build`、`postinstall`。
- 能判断一个工具应该放在根依赖、子包依赖，还是内部配置包依赖里。

## 2. Vben Admin Pro 的技术栈地图

Vben Admin Pro 不是单个 Vue 项目，而是一套完整的工程体系。它把应用、业务包、基础包、配置包、命令工具都放进同一个 monorepo。

### 2.1 包管理与工作区

核心工具：

- Node.js：运行 JS/TS 工具链的基础环境。
- pnpm：包管理器，负责安装依赖、创建 workspace link、执行脚本。
- pnpm workspace：把多个 package 组成一个工作区。
- pnpm catalog：集中管理依赖版本，避免每个包重复写具体版本号。
- `workspace:*`：声明依赖来自当前 monorepo 内部包。

你需要掌握：

- `package.json` 的基本字段。
- `pnpm-workspace.yaml` 的 `packages` 匹配规则。
- `catalog:` 和普通版本号的区别。
- 根依赖和子包依赖的区别。
- `pnpm -r`、`pnpm --filter`、`pnpm exec` 的用法。

### 2.2 Monorepo 任务编排

核心工具：

- Turbo：任务编排和缓存工具。
- turbo tasks：定义 `build`、`dev`、`typecheck` 等任务如何依赖、如何缓存。
- `turbo run build`：按包依赖关系构建。
- `turbo-run`：Vben 内部封装的辅助运行工具。

你需要掌握：

- 为什么 monorepo 不能只靠一个根脚本。
- 什么是任务依赖，例如 `build` 依赖上游包的 `build`。
- 什么是任务输出，例如 `dist/**`。
- 为什么 `dev` 通常不缓存。

### 2.3 TypeScript 配置体系

核心工具：

- TypeScript。
- `tsconfig.json`。
- 共享 tsconfig 包，例如 Vben 的 `@vben/tsconfig`。
- `vue-tsc`：Vue 项目的类型检查。

Vben 的典型分层：

- `base.json`：所有 TS 项目的基础规则。
- `node.json`：Node 工具和配置文件使用。
- `web.json`：浏览器环境包使用。
- `web-app.json`：完整 Web 应用使用。
- `library.json`：需要产出类型声明的库包使用。

你需要掌握：

- `extends` 如何复用配置。
- `compilerOptions` 中常见选项的意义。
- `module`、`moduleResolution`、`target`、`lib` 的关系。
- `noEmit`、`declaration`、`isolatedModules`、`verbatimModuleSyntax` 的意义。
- 为什么应用和库的 tsconfig 不应该完全一样。

### 2.4 包构建与产物

核心工具：

- tsdown：基于 Rolldown 的 TS 库打包工具。
- Rolldown：下一代打包器，定位类似 Rollup。
- `dist`：构建产物目录。
- `.d.ts`：类型声明文件。

Vben 的常见内部包结构：

```txt
internal/vite-config
internal/node-utils
internal/lint-configs/eslint-config
internal/lint-configs/oxlint-config
internal/lint-configs/stylelint-config
```

典型 package 入口：

```json
{
  "type": "module",
  "main": "./dist/index.mjs",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs"
    }
  }
}
```

你需要掌握：

- 为什么 TS 源码不能直接当 Node 包入口。
- 为什么要输出 `dist/index.mjs`。
- 为什么 IDE 跳转依赖 `types` 或 `exports.types`。
- 为什么内部包也要像正式 npm 包一样写入口。
- 什么时候需要 bundle，什么时候只需要 external。

### 2.5 Vite 与前端构建

核心工具：

- Vite。
- Vue 3。
- `@vitejs/plugin-vue`。
- `@vitejs/plugin-vue-jsx`。
- Tailwind CSS。
- Vite 插件体系。

Vben 的做法：

- 把 Vite 配置抽成 `@vben/vite-config`。
- 应用项目只传入差异化配置。
- 公共插件、环境变量、构建优化、压缩、PWA、mock 等逻辑放进内部配置包。

你需要掌握：

- Vite 的 `defineConfig`。
- `plugins` 是如何工作的。
- `resolve.alias` 如何影响路径别名。
- `server`、`build`、`optimizeDeps` 的基本用途。
- 为什么大型项目要抽出共享 Vite 配置。

### 2.6 Lint 与格式化体系

核心工具：

- ESLint：规则检查，尤其是 JS/TS/Vue 代码质量规则。
- ESLint flat config：新版扁平配置方式。
- Oxlint：更快的 lint 工具，适合承担大量基础规则。
- Oxfmt：格式化工具。
- Stylelint：CSS、SCSS、Vue style 块规则。
- Commitlint：提交信息规范。
- CSpell：拼写检查。

Vben 的拆分：

```txt
internal/lint-configs/eslint-config
internal/lint-configs/oxlint-config
internal/lint-configs/oxfmt-config
internal/lint-configs/stylelint-config
internal/lint-configs/commitlint-config
```

你需要掌握：

- ESLint 检查什么，格式化工具检查什么。
- 为什么 ESLint config 也可以做成一个内部 npm 包。
- ESLint 插件、parser、rule 的关系。
- flat config 中 `files`、`ignores`、`languageOptions`、`plugins`、`rules` 的作用。
- 为什么大型项目会让 Oxlint 承担一部分规则，让 ESLint 保留生态插件能力。

### 2.7 Git 工作流与发布管理

核心工具：

- Lefthook：Git hooks 管理。
- Commitlint：提交信息校验。
- czg / cz-git：交互式生成规范 commit。
- Changesets：版本变更和发包记录。

你需要掌握：

- `preinstall`、`postinstall`、`prepare` 脚本的触发时机。
- 为什么要限制只能使用 pnpm。
- 为什么要在提交前跑 lint 或格式化。
- Changesets 如何管理多包版本。

### 2.8 测试与质量检查

核心工具：

- Vitest：单元测试。
- Playwright：端到端测试。
- vue-tsc：Vue 类型检查。
- circular dependency scanner：循环依赖检查。
- dependency checker：依赖声明检查。

你需要掌握：

- 单元测试、组件测试、端到端测试的边界。
- 类型检查和 lint 的区别。
- 依赖检查为什么重要。
- 循环依赖为什么会让大型项目失控。

## 3. 推荐学习顺序

下面的顺序很关键。不要从 ESLint 或 Vite 配置开始抄，先把包和模块解析搞明白。

### 阶段 1：Node 与包基础

先学内容：

- Node.js 是什么。
- npm 包是什么。
- `package.json` 是什么。
- `scripts` 如何执行。
- `dependencies` 和 `devDependencies` 的区别。
- 语义化版本，例如 `^1.2.3`。

为什么先学：

所有 monorepo 配置最终都落在包和脚本上。如果不理解 `package.json`，后面的 workspace、构建、lint 配置都会很抽象。

建议练习：

1. 新建一个最小 package。
2. 写一个 `scripts.hello`。
3. 安装一个依赖并在 JS 文件中 import。
4. 区分运行时代码依赖和开发工具依赖。

学到什么程度：

看到一个 `package.json`，能说出每个字段大概服务于什么。

### 阶段 2：ESM、CJS 与包入口

先学内容：

- CommonJS：`require`、`module.exports`。
- ESM：`import`、`export`。
- `type: "module"`。
- `main`、`module`、`types`。
- `exports` 字段。
- Node 如何解析包名 import。

为什么重要：

你之前遇到的“点击 import 不能跳转”，本质就是包入口和类型入口没配置好。monorepo 里共享配置包、工具包、组件库都会遇到这个问题。

建议练习：

1. 写一个只暴露 `index.js` 的包。
2. 写一个 `exports` 指向不存在文件的包，观察报错。
3. 改成 `dist/index.mjs` 后再 import。
4. 加上 `types`，观察 IDE 跳转变化。

学到什么程度：

看到下面配置时，知道每一行为什么存在：

```json
{
  "type": "module",
  "main": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs"
    }
  }
}
```

### 阶段 3：TypeScript 基础与 tsconfig

先学内容：

- TS 文件如何编译成 JS。
- 类型检查和代码生成的区别。
- `tsconfig.json` 的 `include`、`exclude`、`extends`。
- `compilerOptions.target`。
- `compilerOptions.module`。
- `compilerOptions.moduleResolution`。
- `strict`。
- `declaration`。
- `noEmit`。

为什么重要：

大型 monorepo 中，应用、库、Node 工具、配置文件使用的 TS 规则不完全相同。共享 tsconfig 的目的就是统一基础规则，同时允许不同场景有差异。

建议练习：

1. 写一个 `base.json`。
2. 写一个 `node.json` 继承 `base.json`。
3. 写一个 `web.json` 继承 `base.json`。
4. 对比 `lib: ["ESNext"]` 和 `lib: ["ESNext", "DOM"]`。
5. 打开 `declaration` 生成 `.d.ts`。

学到什么程度：

能解释为什么 Vben 会有：

```txt
@vben/tsconfig/base.json
@vben/tsconfig/node.json
@vben/tsconfig/web.json
@vben/tsconfig/web-app.json
@vben/tsconfig/library.json
```

### 阶段 4：pnpm workspace

先学内容：

- workspace 是什么。
- `pnpm-workspace.yaml` 的 `packages`。
- `workspace:*`。
- `catalog:`。
- `pnpm -r`。
- `pnpm --filter`。
- `pnpm exec`。

为什么重要：

monorepo 的核心不是“多个文件夹”，而是“多个 package 组成一个依赖图”。pnpm workspace 负责把这个依赖图安装、链接、执行起来。

建议练习：

1. 建立 `apps/web` 和 `packages/utils`。
2. 让 `apps/web` 依赖 `@demo/utils`。
3. 使用 `workspace:*`。
4. 把依赖版本放进 `catalog`。
5. 用 `pnpm --filter @demo/web dev` 执行指定包脚本。

学到什么程度：

能判断一个包应该写：

```json
"@demo/utils": "workspace:*"
```

还是：

```json
"typescript": "catalog:"
```

### 阶段 5：Monorepo 目录设计

先学内容：

- `apps`：可运行应用。
- `packages`：可复用业务包或基础包。
- `internal`：只服务当前仓库的内部工具包。
- `scripts`：项目维护脚本。
- `docs`：文档站点。
- `playground`：实验或演示应用。

Vben 的典型分层：

```txt
apps/
packages/
internal/
internal/lint-configs/
internal/tsconfig/
internal/vite-config/
internal/node-utils/
scripts/
docs/
playground/
```

为什么重要：

目录结构不是审美问题，而是边界问题。一个文件夹应该回答：它是否会被发布？是否会被应用依赖？是否只是工程内部使用？

建议练习：

1. 把应用放进 `apps`。
2. 把工具函数包放进 `packages/utils`。
3. 把 eslint 配置放进 `internal/lint-configs/eslint-config`。
4. 把 tsconfig 放进 `internal/tsconfig`。

学到什么程度：

拿到一个需求时，能先判断它属于 app、package、internal config，还是 script。

### 阶段 6：内部配置包设计

先学内容：

- 内部包也是标准 npm 包。
- 配置包为什么要有自己的 `package.json`。
- 配置包为什么要写 `exports`。
- 配置包为什么要有 `dist`。
- `stub` 脚本是什么。

Vben 的模式：

```json
{
  "name": "@vben/eslint-config",
  "private": true,
  "type": "module",
  "scripts": {
    "stub": "pnpm exec tsdown"
  },
  "files": ["dist"],
  "main": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs"
    }
  }
}
```

为什么重要：

共享配置包不只是“放一份配置文件”。它要能被 Node 运行、被 IDE 解析、被其他 workspace 包依赖。

建议练习：

1. 建立 `@demo/eslint-config`。
2. 在根项目安装它：`"@demo/eslint-config": "workspace:*"`。
3. 在根 `eslint.config.mjs` 中 import。
4. 让它从 `src/index.ts` 构建到 `dist/index.mjs`。

学到什么程度：

能自己判断为什么 `exports` 指向 `src/index.ts` 通常不是最佳方案。

### 阶段 7：tsdown 与库构建

先学内容：

- tsdown 的入口文件。
- `format: ["esm"]`。
- `outExtensions`。
- `dts`。
- `deps.skipNodeModulesBundle`。
- `clean`。

Vben 风格配置：

```ts
import { defineConfig } from "tsdown";

export default defineConfig({
  clean: true,
  deps: {
    skipNodeModulesBundle: true,
  },
  dts: {
    resolver: "tsc",
  },
  entry: ["src/index.ts"],
  format: ["esm"],
  outExtensions: () => ({
    dts: ".d.ts",
  }),
});
```

为什么重要：

内部工具包通常是 TS 写的，但 Node 消费的是 JS。tsdown 负责把 TS 包变成标准可消费包。

建议练习：

1. 写一个 `src/index.ts`。
2. 用 tsdown 输出 `dist/index.mjs`。
3. 输出 `dist/index.d.ts`。
4. 在另一个包中 import 它。
5. 尝试 external 依赖，观察产物大小变化。

学到什么程度：

看到 `dist` 产物时，能知道哪些代码被打进去了，哪些依赖被 external 了。

### 阶段 8：ESLint flat config

先学内容：

- ESLint 是什么。
- flat config 和旧版 `.eslintrc` 的区别。
- `files`。
- `ignores`。
- `languageOptions`。
- `plugins`。
- `rules`。
- parser 和 plugin 的关系。

Vben 的思路：

- 把不同领域拆成多个 config 文件。
- `ignores.ts` 管理忽略项。
- `javascript.ts` 管理 JS 基础规则。
- `typescript.ts` 管理 TS parser。
- `vue.ts` 管理 Vue 文件。
- `custom-config.ts` 管理项目特定规则。
- `index.ts` 统一组合。

简化结构：

```txt
src/
  configs/
    ignores.ts
    javascript.ts
    typescript.ts
    node.ts
    index.ts
  custom-config.ts
  index.ts
```

为什么重要：

ESLint 配置一旦变大，如果都写在一个文件里，会很难维护。拆分的本质是领域隔离。

建议练习：

1. 先只写 JS 推荐规则。
2. 再加 ignores。
3. 再加 TS parser。
4. 再加 Node 规则。
5. 最后抽成 `@demo/eslint-config`。

学到什么程度：

能解释为什么一个规则应该放在 `javascript.ts`，而不是放进根 `eslint.config.mjs`。

### 阶段 9：Vite 配置包

先学内容：

- Vite 基础配置。
- Vue 插件。
- 路径别名。
- 环境变量。
- 构建输出。
- 插件封装。

Vben 的做法：

- 把通用 Vite 配置放进 `@vben/vite-config`。
- 应用只选择应用类型配置。
- 重复插件逻辑不散落在每个 app 里。

为什么重要：

当一个仓库有多个前端应用时，每个应用都复制一份 Vite 配置会很快失控。共享配置包可以统一构建策略。

建议练习：

1. 新建 `@demo/vite-config`。
2. 导出一个 `defineApplicationConfig`。
3. 在 app 中 import。
4. 把 alias、plugins、build 配置逐步抽进去。

学到什么程度：

能判断哪些配置应该共享，哪些配置应该留给具体应用。

### 阶段 10：任务编排与 Turbo

先学内容：

- 为什么需要任务编排。
- `turbo.json`。
- `dependsOn`。
- `outputs`。
- cache。
- persistent task。

Vben 的典型任务：

- `build`：构建产物，通常缓存。
- `typecheck`：类型检查，不一定输出文件。
- `dev`：开发服务，不缓存，长期运行。
- `test:e2e`：端到端测试。

为什么重要：

monorepo 中包之间有依赖关系。构建一个 app 前，可能要先构建它依赖的内部包。Turbo 负责把这个顺序自动化。

建议练习：

1. 给每个包加 `build`。
2. 配置 `turbo.json`。
3. 让 app 的 build 依赖上游 package build。
4. 观察第二次构建是否命中缓存。

学到什么程度：

能读懂：

```json
{
  "build": {
    "dependsOn": ["^build"],
    "outputs": ["dist/**"]
  }
}
```

### 阶段 11：Git hooks、提交规范和版本管理

先学内容：

- Git hooks。
- Lefthook。
- Commitlint。
- Conventional Commits。
- czg。
- Changesets。

为什么重要：

大型项目不是只靠代码组织，还要靠流程约束。提交信息、版本记录、发包变更都需要规范化。

建议练习：

1. 配置 commitlint。
2. 用 czg 生成 commit。
3. 用 lefthook 在提交前跑 lint。
4. 用 changesets 生成版本变更记录。

学到什么程度：

能知道哪些检查适合放在提交前，哪些适合放在 CI。

### 阶段 12：质量检查体系

先学内容：

- typecheck。
- lint。
- format。
- test。
- dependency check。
- circular dependency check。
- spell check。

为什么重要：

大型项目的质量不是单靠 ESLint。不同工具负责不同维度：

- TypeScript 负责类型正确性。
- ESLint/Oxlint 负责代码规则。
- Format 工具负责风格一致。
- Test 负责行为正确。
- Dependency check 负责依赖声明正确。
- Circular check 负责架构依赖方向。

建议练习：

1. 分别创建 `lint`、`format`、`typecheck`、`test` 脚本。
2. 不要一开始就把所有检查混成一个命令。
3. 最后再做 `check` 聚合脚本。

学到什么程度：

能设计清楚：

```json
{
  "scripts": {
    "lint": "...",
    "format": "...",
    "typecheck": "...",
    "test": "...",
    "check": "..."
  }
}
```

## 4. 推荐学习节奏

### 第 1 周：包和模块解析

学习重点：

- `package.json`。
- ESM/CJS。
- `exports`。
- pnpm 基础。

产出目标：

- 能创建两个 workspace 包。
- 一个包能 import 另一个包。
- 能解释 import 为什么能跳转。

### 第 2 周：TypeScript 与 tsconfig

学习重点：

- TS 编译。
- `tsconfig` 分层。
- `declaration` 和 `noEmit`。

产出目标：

- 创建 `@demo/tsconfig`。
- app、library、node config 分别继承不同配置。

### 第 3 周：内部包构建

学习重点：

- tsdown。
- `dist`。
- `exports.types`。
- `postinstall` 和 `stub`。

产出目标：

- 创建 `@demo/eslint-config` 或 `@demo/node-utils`。
- TS 源码构建成 JS 和 `.d.ts`。
- 其他包可以正常 import。

### 第 4 周：ESLint 配置包

学习重点：

- ESLint flat config。
- config 拆分。
- parser、plugin、rules。

产出目标：

- 根项目只写：

```js
import { defineConfig } from "@demo/eslint-config";

export default defineConfig();
```

- 所有规则逻辑都收敛进内部配置包。

### 第 5 周：Vite 配置包

学习重点：

- Vite。
- Vue 插件。
- alias。
- env。
- build。

产出目标：

- 创建 `@demo/vite-config`。
- app 中复用共享配置。

### 第 6 周：Turbo 和工程质量

学习重点：

- Turbo 任务编排。
- typecheck。
- lint。
- test。
- commitlint。
- changesets。

产出目标：

- 有一个完整的 `check` 流程。
- 能区分本地开发检查和 CI 检查。

## 5. 每个配置文件应该怎么理解

### 根 package.json

职责：

- 声明仓库名称、Node/pnpm 版本要求。
- 放全局脚本，例如 `dev`、`build`、`check`。
- 放全仓库共享的开发工具，例如 TypeScript、Turbo、tsdown。
- 声明根项目需要直接使用的内部配置包。

不适合做的事：

- 不要把所有业务依赖都塞根目录。
- 不要让根包承担具体 app 的运行依赖。

### 子包 package.json

职责：

- 声明包名。
- 声明包自己的入口。
- 声明包自己的依赖。
- 声明包自己的脚本。

关键判断：

- 这个包运行时 import 了什么，就应该在自己的 `dependencies` 中声明。
- 这个包只在构建时使用的工具，可以放在根目录，由 `pnpm exec` 调用。

### pnpm-workspace.yaml

职责：

- 声明哪些目录属于 workspace。
- 集中管理 catalog 版本。
- 配置 pnpm 的 workspace 行为。

关键判断：

- 新建包后，如果 pnpm 找不到它，先检查 `packages` 匹配规则。
- 多个包使用同一个依赖版本，优先放进 catalog。

### tsconfig.json

职责：

- 告诉 TypeScript 如何理解代码。
- 决定是否生成产物。
- 决定目标环境是 Node、浏览器、库还是应用。

关键判断：

- app 多数情况下 `noEmit: true`，由 Vite 负责构建。
- library 需要 `declaration: true`。
- Node 工具不需要 DOM 类型。
- Web 应用需要 DOM 和 Vite client 类型。

### eslint.config.mjs

职责：

- 作为项目 ESLint 入口。
- 从共享配置包引入规则。
- 尽量保持薄。

理想形态：

```js
import { defineConfig } from "@demo/eslint-config";

export default defineConfig();
```

### tsdown.config.ts

职责：

- 定义内部 TS 包如何构建。
- 决定入口、输出格式、类型声明、external 策略。

关键判断：

- 配置包通常输出 ESM。
- 内部依赖和第三方依赖通常不打进配置包产物。
- `.d.ts` 路径要和 `package.json` 的 `types` 对齐。

## 6. 常见误区

### 误区 1：把 monorepo 当成多个文件夹

正确理解：

monorepo 是多个 package 组成的依赖图。核心是 package 边界，不是文件夹数量。

### 误区 2：根目录安装所有依赖

正确理解：

根目录放全仓库工具。具体包运行时需要的依赖，应放在具体包里。

### 误区 3：直接把 exports 指向 src/index.ts

正确理解：

Node 默认不能直接执行 TS。内部包最好构建到 `dist`，再让 `exports` 指向构建产物。

### 误区 4：一开始就抄完整 Vben 配置

正确理解：

Vben 的配置是多年演化出来的。学习时应该先做最小闭环，再逐步加规则和工具。

### 误区 5：ESLint、TypeScript、格式化工具职责混淆

正确理解：

- TypeScript 检查类型。
- ESLint 检查代码规则和潜在问题。
- Format 工具统一排版。
- Test 验证行为。

### 误区 6：共享配置包没有类型入口

正确理解：

没有 `types` 或 `exports.types`，IDE 跳转和类型提示会变差。

## 7. 推荐从零搭建的最小路线

不要一开始就做完整 Vben。建议按这个最小版本实现：

```txt
apps/
  frontend/
  backend/
packages/
  utils/
internal/
  tsconfig/
  lint-configs/
    eslint-config/
pnpm-workspace.yaml
package.json
eslint.config.mjs
```

第一阶段只实现：

- pnpm workspace。
- `@demo/tsconfig`。
- `@demo/eslint-config`。
- 根 `eslint.config.mjs` 引入共享 ESLint 配置。
- `postinstall` 自动跑 `stub`。

第二阶段再加入：

- `@demo/vite-config`。
- Turbo。
- typecheck。
- test。

第三阶段再加入：

- oxlint。
- stylelint。
- commitlint。
- changesets。
- dependency check。
- circular dependency check。

## 8. 判断一个设计是否合理的标准

设计内部包时问自己：

1. 这个包的职责是否单一？
2. 这个包是否可以被其他 workspace 包清晰 import？
3. 它的 `exports` 是否指向真实存在的产物？
4. 它的类型入口是否正确？
5. 它运行时 import 的依赖是否写进了自己的 `dependencies`？
6. 构建工具是否可以放根目录复用？
7. 根配置文件是否足够薄？
8. 是否避免了为了未来需求提前引入过多工具？

## 9. Vben Admin Pro 值得学习的架构思想

### 配置即包

Vben 把 ESLint、Stylelint、Oxlint、Vite、tsconfig 都做成内部 package。这让配置可以被版本化、被依赖、被复用、被 IDE 解析。

### 根入口保持薄

根 `eslint.config.mjs`、`stylelint.config.mjs`、`oxlint.config.ts` 都很薄，只负责 import 内部配置包。

### 内部工具集中管理

`internal/node-utils`、`internal/vite-config`、`internal/tsconfig` 都是工程能力，不是业务代码。它们放在 `internal` 下，边界清楚。

### 依赖版本集中

pnpm catalog 让所有包使用统一版本，减少版本漂移。

### 构建产物标准化

内部 TS 包基本都输出 `dist/index.mjs` 和 `dist/index.d.ts`，让 Node 和 IDE 都能稳定消费。

### 任务编排清晰

Turbo 负责任务关系，package scripts 负责具体动作，pnpm workspace 负责包定位。这三者职责不同。

## 10. 你现在最应该先掌握的 10 个概念

按优先级排序：

1. `package.json`
2. ESM 和 CJS
3. `exports`
4. `dependencies` 和 `devDependencies`
5. pnpm workspace
6. `workspace:*`
7. pnpm catalog
8. `tsconfig extends`
9. tsdown 构建 `dist`
10. ESLint flat config

如果这 10 个概念理解了，Vben 这类 monorepo 的一半“神秘感”就会消失。

## 11. 推荐阅读源码顺序

阅读 Vben Admin Pro 时，建议按这个顺序：

1. 根 `package.json`
2. `pnpm-workspace.yaml`
3. `turbo.json`
4. `internal/tsconfig`
5. `internal/lint-configs/eslint-config/package.json`
6. `internal/lint-configs/eslint-config/tsdown.config.ts`
7. `internal/lint-configs/eslint-config/src/index.ts`
8. `internal/lint-configs/eslint-config/src/configs/*`
9. 根 `eslint.config.mjs`
10. `internal/vite-config/package.json`
11. `internal/vite-config/src/index.ts`
12. 一个具体 app 的 `vite.config.ts`
13. 一个具体 app 的 `package.json`
14. 一个 packages 下的库包

不要从业务页面开始看。先看工程骨架，再看业务代码。

## 12. 实操练习清单

### 练习 1：最小 workspace

目标：

- 创建根 package。
- 创建 `apps/web`。
- 创建 `packages/utils`。
- 让 app import utils。

验收：

- `pnpm i` 后 workspace link 正常。
- app 可以运行 utils 的函数。

### 练习 2：共享 tsconfig

目标：

- 创建 `internal/tsconfig`。
- 提供 `base.json`、`node.json`、`web.json`。
- app 和 package 分别 extends。

验收：

- `tsc --noEmit` 能跑。
- 改错类型时能报错。

### 练习 3：共享 ESLint config

目标：

- 创建 `internal/lint-configs/eslint-config`。
- 使用 tsdown 构建。
- 根 `eslint.config.mjs` 引入它。

验收：

- `pnpm i` 能生成 dist。
- `pnpm exec eslint .` 能跑。
- IDE 可以跳转到类型或源码。

### 练习 4：共享 Vite config

目标：

- 创建 `internal/vite-config`。
- app 中复用它。

验收：

- app 能启动。
- alias 和插件从共享配置生效。

### 练习 5：任务编排

目标：

- 加入 Turbo。
- 配置 `build` 和 `typecheck`。

验收：

- `turbo build` 能按依赖顺序执行。
- 第二次构建能看到缓存效果。

## 13. 最小可行架构示例

根 `package.json`：

```json
{
  "name": "demo-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "pnpm -r run --if-present stub",
    "lint": "eslint .",
    "typecheck": "pnpm -r run --if-present typecheck"
  },
  "devDependencies": {
    "@demo/eslint-config": "workspace:*",
    "@demo/tsconfig": "workspace:*",
    "eslint": "catalog:",
    "tsdown": "catalog:",
    "typescript": "catalog:"
  },
  "packageManager": "pnpm@11.7.0"
}
```

`pnpm-workspace.yaml`：

```yaml
packages:
  - apps/*
  - packages/*
  - internal/*
  - internal/lint-configs/*

catalog:
  eslint: ^10.5.0
  tsdown: ^0.22.3
  typescript: ^6.0.3
```

根 `eslint.config.mjs`：

```js
import { defineConfig } from "@demo/eslint-config";

export default defineConfig();
```

内部 ESLint 包 `package.json`：

```json
{
  "name": "@demo/eslint-config",
  "private": true,
  "type": "module",
  "scripts": {
    "stub": "pnpm exec tsdown"
  },
  "files": ["dist"],
  "main": "./dist/index.mjs",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs"
    }
  },
  "dependencies": {
    "@eslint/js": "catalog:",
    "eslint": "catalog:"
  }
}
```

## 14. 最后建议

学习 Vben 这种架构，最重要的不是记住每个工具的配置项，而是理解边界：

- 什么是应用边界。
- 什么是包边界。
- 什么是内部工具边界。
- 什么是运行时依赖。
- 什么是开发时依赖。
- 什么应该被构建。
- 什么应该只在源码中存在。
- 什么应该放根目录。
- 什么应该放子包。

先做小，再做全。先让一个最小 monorepo 跑通，再逐步加 tsconfig、eslint、vite、turbo、commitlint、changesets。这样你学到的是架构能力，而不是复制配置。
